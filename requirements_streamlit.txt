# Streamlit web app requirements
streamlit>=1.28.0
streamlit-folium>=0.15.0
plotly>=5.15.0
folium>=0.14.0

# Core GeoMasterPy dependencies
earthengine-api>=0.1.300
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
requests>=2.28.0
pillow>=9.0.0
geojson>=2.5.0

# Geospatial libraries (lighter versions for web deployment)
shapely>=1.8.0
geopandas>=0.12.0
rasterio>=1.3.0

# Optional heavy dependencies (commented out to reduce build time)
# cartopy>=0.21.0  # Heavy dependency - only needed for publication maps
# ipyleaflet>=0.17.0  # Not needed for Streamlit web app
# ipywidgets>=8.0.0  # Not needed for Streamlit web app

# Additional web app dependencies
watchdog>=3.0.0
click>=8.0.0
altair>=4.0.0  # For Streamlit compatibility
protobuf>=3.19.5  # For Earth Engine
urllib3>=1.21.1
certifi>=2017.4.17